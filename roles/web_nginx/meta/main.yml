---
galaxy_info:
  author: home-ansible
  description: "Nginx static file server — multi-site, autoindex support, per-host document roots"
  min_ansible_version: "2.10"
dependencies:
  - role: base_packages
