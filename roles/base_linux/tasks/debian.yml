---
# BUG FIXED: apt update / git / python3 removed — now handled by base_packages
#            dependency declared in meta/main.yml.
# BUG FIXED: upgrade=dist (legacy kv) → upgrade: dist
# BUG FIXED: autoremove=yes → autoremove: true

- name: Upgrade all packages to latest
  ansible.builtin.apt:
    upgrade: dist
    update_cache: true
    cache_valid_time: 3600
  become: true
  tags: [base_linux, upgrade]
