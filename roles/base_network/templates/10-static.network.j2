# /etc/systemd/network/10-static.network — managed by Ansible (base_network role)
# Used on hosts where systemd-networkd is active without netplan.
[Match]
Name=eth0

[Network]
Address={{ static_ip }}/24
Gateway={{ home_gateway }}
DNS={{ home_dns_primary }}
DNS={{ home_dns_fallback }}
