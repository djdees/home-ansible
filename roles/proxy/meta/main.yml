---
galaxy_info:
  role_name: proxy
  description: >
    Nginx reverse proxy in Docker using cgr.dev/chainguard/nginx.
    Terminates TLS, redirects HTTP to HTTPS, and proxies internal services.
    Designed to support Vault-issued certs when available.
  min_ansible_version: "2.14"
dependencies: []
