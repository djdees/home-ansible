---
# BUG FIXED: apt update / python3 / git removed — handled by base_packages.
# BUG FIXED: python-pip (Python 2 EOL, removed from Debian 12) removed.

- name: Install dev tools
  ansible.builtin.apt:
    name:
      - tmux
      - yamllint
      - jq
      - build-essential
      - shellcheck
    state: present
  become: true
  tags: [dev, install]
