---
# playbooks/samba-setup.yml
# Installs and configures Samba on print servers.
#
# Usage:
#   ansible-playbook playbooks/samba-setup.yml

- hosts: pi_servers
  become: true
  roles:
    - base_samba
