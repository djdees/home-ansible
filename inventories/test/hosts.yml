---
# inventories/test/hosts.yml
# Staging / test hosts. Use with: ansible-playbook -i inventories/test/hosts.yml
all:
  children:
    lab:
      hosts:
        pi04:
          ansible_host: 192.168.1.94
          ansible_user: pi
          ansible_group: pi
