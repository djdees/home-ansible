---
# playbooks/base-docker.yml
# Installs Docker CE with the correct Debian repo URLs.
#
# Usage:
#   ansible-playbook playbooks/base-docker.yml
#   ansible-playbook playbooks/base-docker.yml --limit infra_servers

- hosts: all
  become: true
  roles:
    - base_docker
